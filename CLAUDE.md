# Junior-Invest — הכתובת הישנה של תיק ההשקעות

**האפליקציה כבר לא כאן.** הקוד עבר לריפו `lironcon2015-prog/homebudget` תחת
`invest/`, ומוגש מ-<https://homebudget.lironcon.com/invest/>.

## למה הריפו הזה עדיין קיים

`invest.lironcon.com` מצביע לכאן (דרך `CNAME`), ואי אפשר פשוט לכבות אותו:

1. **נתונים.** `localStorage` הוא לפי origin, ולכן נתונים שנשמרו בכתובת הזו לא
   עוברים לכתובת החדשה מעצמם. `index.html` כאן הוא דף העברה: הוא קורא את כל
   מפתחות `juniorinvest:*`, דוחס אותם ומעביר אותם ב-fragment של ה-URL — לא
   ב-query string, כדי שהם לא יירשמו בלוגים בדרך. הצד המקבל יושב ב-bootstrap
   של `invest/index.html` בריפו החדש; **שני הצדדים חייבים להשתנות יחד.**
2. **סימניות ואפליקציות מותקנות.** PWA שהותקנה מהכתובת הזו ממשיכה לפתוח אותה,
   ולכן הדף חייב להישאר עובד לצמיתות ולא רק לתקופת מעבר.

## מה מותר לשנות כאן

רק את דף ההעברה. אין להחזיר לכאן קוד אפליקציה — שני עותקים חיים של אותה
אפליקציה על שני origins הם בדיוק הבעיה שההעברה באה לפתור, והם ייצרו שני מאגרי
נתונים שמתפצלים בשקט.

לפיתוח האפליקציה עצמה: `invest/CLAUDE.md` בריפו `homebudget`.

## בדיקה מקומית

`python3 -m http.server` ואז פתיחה בדפדפן. הדף מקודד את מה שנמצא ב-`localStorage`
של אותו origin, אז כדי לבדוק אותו באמת צריך קודם לזרוע שם מצב.
